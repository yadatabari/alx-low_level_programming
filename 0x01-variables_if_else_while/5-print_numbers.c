#include <stdio.h>
/**
 * main - entry point
 * Return: always (0)
 */
int main(void)
{
	int a;
	/*for loop will check the condition*/
	for (a = 0 ; a < 10 ; a++)
	{
		printf("%d", a);
	}
	printf("\n");
		return (0);
}
