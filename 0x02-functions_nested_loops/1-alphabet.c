#include "main.h"
/**
 * print_alphabet - make the alphabet
 * Return: void
 */
void print_alphabet(void)
{
	char i;
	/*For loop will check the condition*/
	for (i = 'a' ; i <= 'z' ; i++)
		_putchar(i);
	_putchar('\n');
}
